[pytest]
# Pytest configuration for Competitive Intelligence System

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Minimum Python version
minversion = 3.10

# Add src to Python path
pythonpath = src

# Console output options
addopts = 
    # Verbose output
    -v
    # Show extra test summary info
    -ra
    # Show local variables on failure
    --tb=short
    # Strict markers (fail on unknown markers)
    --strict-markers
    # Disable warnings summary
    -p no:warnings

# Custom markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    tracing: marks tests related to Arize AX tracing
    asyncio: marks tests as async

# Asyncio configuration
asyncio_mode = auto

# Timeout settings (requires pytest-timeout)
timeout = 600
timeout_method = thread

# Log configuration
log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Ignore paths
norecursedirs = 
    .git
    .tox
    dist
    build
    *.egg
    __pycache__
    .venv
    venv

# Coverage options (requires pytest-cov)
# Uncomment to enable coverage reporting
# addopts = 
#     --cov=src/ai_poc/workflow_1
#     --cov-report=html
#     --cov-report=term-missing
#     --cov-branch
